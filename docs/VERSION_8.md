# Версия 8 — нормализованная база и ребрендинг

## База данных

- прежнее JSON-хранилище заменено нормализованной схемой Cloudflare D1;
- добавлены таблицы сотрудников, продуктов, заявок, оплат, планов и условий мотивации;
- добавлена история перемещения заявок по Kanban;
- добавлен журнал значимых изменений;
- созданы внешние ключи, проверки денежных значений и составные индексы;
- реализован первоначальный импорт 174 заявок и 95 оплат;
- API чтения и сохранения переведён на новые таблицы;
- legacy-таблица сохранена для обратимой миграции.

## Интерфейс и бренд

- FinFlow CRM переименована в Cursor CRM;
- обновлены заголовки страниц, логины демонстрационных пользователей и README;
- создан новый SVG-фавикон с символом C и линией динамики продаж;
- удалены старые упоминания и артефакты FinFlow.

## Локальная разработка

- добавлена конфигурация Wrangler для локальной D1;
- создана и проверена миграция `0001_wise_fabian_cortez.sql`;
- добавлена подробная ER-схема в `docs/DATABASE_SCHEMA.md`;
- подтверждена работа GET/PUT API;
- подтверждено использование индексов для фильтрации заявок и оплат;
- production-сборка проходит успешно;
- приложение запускается на `http://localhost:3000`.

## GitHub

- версия размещается в отдельной ветке `версии`;
- ветка `main` не изменяется;
- в GitHub передаётся обезличенный демонстрационный набор данных.
